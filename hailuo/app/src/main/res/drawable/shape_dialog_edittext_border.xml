<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 设置背景色 -->
    <solid android:color="@color/global_bg_white" />

    <!-- 设置边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/main_bottom_line_color" />

    <!-- 设置四个圆角的半径 -->
    <corners
        android:radius="@dimen/dp_4" />

</shape>